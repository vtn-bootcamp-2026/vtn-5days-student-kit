# Fallback Inputs — Session 2

> Sample outputs GV cấp cho nhóm chậm/không kịp, để tiếp tục session sau. Tương đương "checkpoint rescue" trong môi trường no-code.

**Tình huống:** Cung cấp design doc mẫu (HCNS/Marketing) để HV điền vào.

**Output kỳ vọng (mẫu):** Workflow design doc (.md) + 1 ảnh sơ đồ workflow.

> File mẫu cụ thể nằm trong `synthetic-data/` (đánh dấu `-sample`).